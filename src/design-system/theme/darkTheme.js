import { darkColors, darkShadows, motion, radius, spacing, typography } from '../tokens';

export const darkTheme = Object.freeze({
  mode: 'dark',
  colors: darkColors,
  typography,
  spacing,
  radius,
  shadows: darkShadows,
  motion,
});

