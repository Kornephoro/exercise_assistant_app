import { lightColors, lightShadows, motion, radius, spacing, typography } from '../tokens';

export const lightTheme = Object.freeze({
  mode: 'light',
  colors: lightColors,
  typography,
  spacing,
  radius,
  shadows: lightShadows,
  motion,
});

